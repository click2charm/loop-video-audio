# 🔐 คู่มือการติดตั้งระบบ License

## ภาพรวม
แอป Loop Video to Audio มีระบบ License ดังนี้:
- ✅ ทดลองใช้ฟรี 14 วัน
- ✅ License แบบ Lifetime (ตลอดชีพ)
- ✅ ผูกกับเครื่องอัตโนมัติ (ป้องกันแชร์)
- ✅ ตรวจสอบ Online ผ่าน Firebase

---

## 📝 ขั้นตอนการติดตั้ง

### ขั้นตอนที่ 1: ตั้งค่า Firebase (5 นาที)

#### 1.1 เปิดใช้งาน Realtime Database

1. ไปที่ https://console.firebase.google.com
2. เลือก Project: **loop-video-to-audio**
3. คลิก **Realtime Database** (เมนูซ้าย)
4. คลิก **Create Database** (ถ้ายังไม่มี)
5. เลือก Location: **us-central1** (หรือใกล้คุณที่สุด)
6. Start in **Test Mode** (เราจะตั้ง rules ใหม่ทีหลัง)
7. คลิก **Enable**

#### 1.2 ตั้งค่า Security Rules

1. ใน Realtime Database → คลิกแท็บ **Rules**
2. แทนที่ด้วย rules นี้:

```json
{
  "rules": {
    "licenses": {
      ".read": true,
      "$licenseKey": {
        ".write": "!data.exists() || data.child('status').val() === 'available'"
      }
    }
  }
}
```

3. คลิก **Publish**

**อธิบาย:**
- `.read: true` - แอปอ่านได้ (เช็ค license)
- `.write` - เขียนได้แค่ตอน activate ครั้งแรก (status = available)

#### 1.3 หา Web API Key

1. ไปที่ **Project Settings** (⚙️ มุมซ้ายบน)
2. ไปที่แท็บ **General**
3. เลื่อนลงหา **Web API Key**
4. คัดลอก API Key (เช่น `AIzaSyB...`)
5. **เก็บไว้ - จะใช้ในขั้นตอนที่ 2**

---

### ขั้นตอนที่ 2: สร้างและอัปโหลด License Keys (10 นาที)

คุณมี **3 วิธีเลือก** (เลือก 1 วิธีที่ถนัด):

#### 🌐 วิธี A: ใช้ Web Generator (แนะนำ - ไม่ต้องติดตั้งอะไร)

1. Pull code ล่าสุด:
```bash
git pull origin claude/debug-repo-issues-011CUtYtTtwp9LAMWr8Yaeqc
```

2. เปิดไฟล์ในเบราว์เซอร์:
```bash
open generate-licenses-web.html  # macOS
# หรือ
start generate-licenses-web.html  # Windows
# หรือ double-click ไฟล์
```

3. กรอกข้อมูล:
   - **Firebase Project ID:** `loop-video-to-audio`
   - **Firebase API Key:** (ที่ได้จากขั้นตอนที่ 1.3)
   - **SECRET_KEY:** `YOUR-SECRET-KEY-CHANGE-THIS-2024`
   - **จำนวน License Keys:** `10000`

4. คลิก **"สร้าง License Keys"** (รอ 1-2 นาที)

5. คลิก **"ดาวน์โหลด JSON"** (backup ไว้)

6. คลิก **"อัปโหลดไป Firebase"** (รอ 2-3 นาที)

7. เสร็จ! ✅

---

#### 💻 วิธี B: ใช้ Firebase Console (ง่ายที่สุด - แต่ต้องมีไฟล์)

**ถ้าคุณมีไฟล์ `license_keys.json` แล้ว:**

1. ไปที่ https://console.firebase.google.com
2. เลือก project: **loop-video-to-audio**
3. ไปที่ **Realtime Database**
4. คลิก **⋮** (มุมขวาบน) → **Import JSON**
5. เลือกไฟล์ `license_keys.json`
6. คลิก **Import**
7. เสร็จ! ✅

**ถ้ายังไม่มีไฟล์:** ใช้วิธี A หรือ C เพื่อสร้างก่อน

---

#### 🖥️ วิธี C: ใช้ Node.js Script

```bash
# 1. Pull code
git pull origin claude/debug-repo-issues-011CUtYtTtwp9LAMWr8Yaeqc

# 2. สร้าง license keys
node generate-licenses.js 10000
# จะได้ไฟล์ license_keys.json (2.2 MB)

# 3. ตั้งค่า API Key
export FIREBASE_API_KEY="AIzaSy..." # ที่ได้จากขั้นตอนที่ 1.3

# 4. อัปโหลด
node upload-to-firebase.js
```

---

### ขั้นตอนที่ 3: Build และแจกจ่ายแอป (5 นาที)

```bash
# 1. Pull code ล่าสุด (ถ้ายังไม่ได้ pull)
git pull origin claude/debug-repo-issues-011CUtYtTtwp9LAMWr8Yaeqc

# 2. ติดตั้ง dependencies (ถ้ายังไม่ได้ติดตั้ง)
npm install

# 3. Build แอป
npm run build

# 4. ไฟล์ที่ได้จะอยู่ใน dist/
# - macOS: dist/*.dmg หรือ dist/mac/*.app
# - Windows: dist/*.exe
```

---

## 🎮 วิธีใช้งาน (สำหรับผู้ใช้):

1. เปิดแอป → ทดลองใช้ฟรี 14 วัน
2. หมด trial → แสดงหน้าต่างกรอก License
3. ติดต่อซื้อ License ที่:
   - 📘 Facebook: facebook.com/promptmuseautomate
   - 💬 Line ID: @xmz6911f
4. กรอก License Key → ผูกกับเครื่องอัตโนมัติ
5. ใช้งานได้ตลอดชีพ! 🎉

---

## 💰 วิธีขาย License (สำหรับคุณ):

### ตรวจสอบ License Keys ที่มี

ไปที่: Firebase Console > Realtime Database > Data

คุณจะเห็นโครงสร้างดังนี้:

```
licenses/
  ├─ XXXX_XXXX_XXXX_XXXX_XXXX_XXXX
  │   ├─ status: "available"      ← ยังไม่ได้ขาย
  │   ├─ created: "2025-11-11..."
  │   └─ ...
  │
  ├─ YYYY_YYYY_YYYY_YYYY_YYYY_YYYY
  │   ├─ status: "activated"      ← ขายแล้ว
  │   ├─ machineId: "abc123..."
  │   ├─ machineName: "DESKTOP-..."
  │   └─ activated: "2025-11-11..."
```

### ขั้นตอนการขาย

1. **ลูกค้าติดต่อ** Facebook/Line
2. **คุณเปิด** `license_keys.json` หรือ Firebase Console
3. **หา key** ที่ `status: "available"`
4. **ส่ง License Key** ให้ลูกค้า (รูปแบบ: `XXXX-XXXX-XXXX-XXXX-XXXX-XXXX`)
   - ⚠️ **เปลี่ยน `_` เป็น `-`** (underscore → dash)
   - ✅ ถูกต้อง: `1VFY-1SYS-A5C8-L0V0-7FMF-3716`
   - ❌ ผิด: `1VFY_1SYS_A5C8_L0V0_7FMF_3716`
5. **ลูกค้ากรอก** → ระบบผูกกับเครื่องอัตโนมัติ
6. **ใน Firebase** จะเห็น `status` เปลี่ยนเป็น `"activated"`

---

## 🔍 ตรวจสอบการใช้งาน

### ใน Firebase Console

1. Realtime Database > Data
2. กรอง: `status: "activated"` = ขายแล้ว
3. กรอง: `status: "available"` = ยังไม่ได้ขาย
4. ดู `lastCheck` = ครั้งสุดท้ายที่ลูกค้าเปิดแอป

### สถิติ

```
Total keys: 10,000
Available: 9,950 (99.5%)
Activated: 50 (0.5%)
```

---

## ⚠️ คำเตือนความปลอดภัย

### ไฟล์ที่ห้ามแชร์:

- ❌ `firebase-admin-key.json` - Private key ของ Firebase
- ❌ `license_keys.json` - License keys ทั้งหมด
- ❌ `.env` - Environment variables
- ✅ อยู่ใน `.gitignore` แล้ว

### SECRET_KEY

- อยู่ใน `license.js` บรรทัดที่ 10
- อยู่ใน `generate-licenses.js` บรรทัดที่ 22
- **ห้าม** แชร์หรือเปลี่ยน (จะทำให้ license keys เก่าใช้ไม่ได้)

---

## 🐛 แก้ปัญหา

### ปัญหา: อัปโหลด Firebase ล้มเหลว (Permission denied)

**วิธีแก้:**
1. Firebase Console > Realtime Database > Rules
2. เปลี่ยนชั่วคราวเป็น:
```json
{
  "rules": {
    ".read": true,
    ".write": true
  }
}
```
3. อัปโหลดอีกครั้ง
4. เปลี่ยน Rules กลับเป็นแบบปลอดภัย (ตามขั้นตอนที่ 1.2)

### ปัญหา: ลูกค้ากรอก License แล้วขึ้น "ไม่พบในระบบ"

**สาเหตุ:**
- ส่ง key ผิดรูปแบบ (ต้องใช้ `-` ไม่ใช่ `_`)
- License ยังไม่ได้อัปโหลดไป Firebase

**วิธีแก้:**
- เช็คใน Firebase Console ว่ามี key นั้นหรือไม่
- ตรวจสอบว่าส่ง format ถูกต้อง (XXXX-XXXX-... ไม่ใช่ XXXX_XXXX_...)

### ปัญหา: ลูกค้ากรอกแล้วขึ้น "License key ถูกใช้งานแล้ว"

**คำอธิบาย:**
- License ถูกใช้ไปแล้ว (1 license = 1 เครื่อง)
- ลูกค้าพยายามใช้บนเครื่องที่ 2

**วิธีแก้:**
- ขาย license ใหม่
- หรือ reset license เดิมใน Firebase (ไม่แนะนำ)

---

## ✅ Checklist สรุป

- [ ] ตั้งค่า Firebase Realtime Database
- [ ] ตั้งค่า Security Rules
- [ ] สร้าง License Keys (10,000 keys)
- [ ] อัปโหลด Keys ไป Firebase
- [ ] ทดสอบ: เปิดแอป → trial 14 วัน
- [ ] ทดสอบ: กรอก License Key → activate สำเร็จ
- [ ] Build แอป
- [ ] แจกจ่าย/ขาย!

---

## 📞 ช่องทางสั่งซื้อ License

- 📘 **Facebook:** https://www.facebook.com/promptmuseautomate
- 💬 **Line ID:** @xmz6911f

---

**อัปเดตล่าสุด:** 2025-11-11
**Version:** 1.0.1
